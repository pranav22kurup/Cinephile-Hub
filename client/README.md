# Cinephile Hub Frontend

React + Vite + Material-UI with JWT auth and role-based protected routes.

## Scripts
```bash
cd client
npm install
npm run dev
```

Create a `.env` file or define `VITE_API_URL` pointing to the backend, e.g. `http://localhost:4000`.
